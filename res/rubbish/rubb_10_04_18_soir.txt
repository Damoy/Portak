// function keyPressed(evt) {
//   let player = WorldContext.getMainPlayer();
//   switch(evt.code) {
//     case 'ArrowRight':
//       player.changeDirection(PlayerContext.getRightDirValue());
//       // player.setSpeedX(player.getMaxSpeed());
//       break;
//     case 'ArrowLeft':
//       player.changeDirection(PlayerContext.getLeftDirValue());
//       // player.setSpeedX(-player.getMaxSpeed());
//       break;
//     case 'ArrowUp':
//       player.changeDirection(PlayerContext.getUpDirValue());
//       // player.setSpeedY(-player.getMaxSpeed());
//       break;
//     case 'ArrowDown':
//       player.changeDirection(PlayerContext.getDownDirValue());
//       // player.setSpeedY(player.getMaxSpeed());
//       break;
//   }
// }

// function keyReleased(evt) {
//   let player = WorldContext.getMainPlayer();
//   switch(evt.code) {
//     case 'ArrowRight':
//     case 'ArrowLeft':
//       // player.changeDirection(PlayerContext.getNoneDirValue());
//       player.stop();
//       break;
//     case 'ArrowUp':
//     case 'ArrowDown':
//       // player.changeDirection(PlayerContext.getNoneDirValue());
//       player.stop();
//       break;
//   }
// }

    // else if(key == KeyEvent.VK_ENTER) keyState[ENTER] = b;
    // else if(key == KeyEvent.VK_ESCAPE) keyState[ESCAPE] = b;
    // else if(key == KeyEvent.VK_SPACE) keyState[SPACE] = b;
    // else if(key == KeyEvent.VK_P) keyState[KEY_P] = b;
    // else if(key == KeyEvent.VK_Z) keyState[KEY_Z] = b;
    // else if(key == KeyEvent.VK_W) keyState[KEY_W] = b;
    // else if(key == KeyEvent.VK_Q) keyState[KEY_Q] = b;
    // else if(key == KeyEvent.VK_A) keyState[KEY_A] = b;
    // else if(key == KeyEvent.VK_S) keyState[KEY_S] = b;
    // else if(key == KeyEvent.VK_D) keyState[KEY_D] = b;
    // else if(key == KeyEvent.VK_O) keyState[KEY_O] = b;
    // else if(key == KeyEvent.VK_L) keyState[KEY_L] = b;
    // else if(key == KeyEvent.VK_M) keyState[KEY_M] = b;
    // else if(key == KeyEvent.VK_E) keyState[KEY_E] = b;
    // else if(key == KeyEvent.VK_R) keyState[KEY_R] = b;



    					if(tile.isOccupied()){
						this.tileDestReached = true;
						this.lastDx = 0;
						this.lastDy = 0;
						this.direction = PlayerContext.getNoneDirValue();
						this.blocked = true;
						this.xSave = this.x;
						this.ySave = this.y;
						this.lastDx = this.beforeLastDx;
						this.lastDy = this.beforeLastDy;
						return false;

												// this.xSave = this.x;
						// this.ySave = this.y;
						// this.lastDx = this.beforeLastDx;
						// this.lastDy = this.beforeLastDy;